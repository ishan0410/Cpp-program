#include <iostream>

using namespace std;

int main()
{
    int arr[5];
    cout << "Enter the elements :" << endl;
    for (int i=0;i<=4;++i)
    cin>>arr[i];

    cout << "You entered :" << endl;
    for (int i=0;i<=4;++i)
        cout<<*(arr+i)<<endl;
    return 0;
}
